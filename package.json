{
  "name": "freelancer",
  "version": "0.0.3",
  "description": "Website for freelancers with homepage and payments pages to securely receive payments for services",
  "private": true,
  "dependencies": {
    "async": "^2.6.1",
    "body-parser": "^1.18.3",
    "compression": "^1.7.3",
    "csurf": "^1.9.0",
    "debug": "^4.1.0",
    "ejs": "^2.6.1",
    "express": "^4.16.4",
    "helmet": "^3.15.0",
    "morgan": "^1.9.1",
    "nodemailer": "^4.7.0",
    "nodemailer-mailgun-transport": "^1.4.0",
    "response-time": "^2.3.2",
    "serve-static": "^1.13.2",
    "server-error-pages": "0.0.4",
    "stripe": "^6.19.0",
    "vhost": "^3.0.2"
  },
  "devDependencies": {
    "nodemon": "^1.18.9",
    "snyk": "^1.118.0",
    "supertest": "^3.3.0"
  },
  "scripts": {
    "start": "export NODE_ENV=${NODE_ENV=development}; if [ $NODE_ENV = 'production' ]; then npm run start:prod; else npm run start:dev; fi",
    "start:dev": "npm run start:dev:client & npm run start:dev:server",
    "start:dev:client": ". ./.env.dev; nodemon --watch lib --ext 'html,css' --exec 'npm run build'",
    "start:dev:server": ". ./.env.dev; nodemon $NODE_DEBUG_OPTION ./bin/www",
    "start:prod": "if [ -e ./.env.prod ]; then . ./.env.prod; fi; npm run build; node $NODE_DEBUG_OPTION ./bin/www",
    "build": "./bin/build.sh",
    "setup": "./bin/setup.sh",
    "snyk:wizard": "snyk wizard"
  },
  "engines": {
    "node": "10.11.0"
  },
  "author": "Mark Smith"
}
